# qy-update-claude-rules

安全地修改 CLAUDE.md 文档

---

## 功能说明

**触发词：** 修改规则、更新 CLAUDE.md、修改 CLAUDE 文档

**目的：** 确保 CLAUDE.md 修改符合规范，避免随意修改导致系统混乱

---

## 执行流程

### 步骤 1：判断修改位置

**问用户：**
```
要修改哪个 CLAUDE.md？

[1] 根目录 CLAUDE.md（D:\一人公司OS\CLAUDE.md）
[2] 项目级 CLAUDE.md（Projects/{项目名}/CLAUDE.md）
[3] 领域级 CLAUDE.md（Areas/{领域名}/CLAUDE.md）
```

### 步骤 2：读取"何时修改"规则

读取 `D:\一人公司OS\CLAUDE.md` 中的"何时修改 CLAUDE.md"板块。

**判断标准：**
- 影响**所有用户/所有项目** → 根目录 CLAUDE.md
- 只影响**某个项目/某个领域** → 子文件夹 CLAUDE.md

### 步骤 3：验证修改合理性

**检查清单：**
- [ ] 修改内容是否符合"何时修改"规则？
- [ ] 修改是否会影响现有功能？
- [ ] 是否需要同步更新其他文档？

**如果不符合规则，提示用户重新考虑。**

### 步骤 4：备份旧版本

**备份命名：**
```
{原文件名}_backup_{YYYYMMDD}_{HHMMSS}.md
```

**示例：**
```
CLAUDE_backup_20260710_153045.md
```

**执行：**
```bash
cp "{原文件路径}" "{备份路径}"
```

### 步骤 5：执行修改

根据用户提供的修改内容，更新 CLAUDE.md 文件。

### 步骤 6：更新变更日志

**如果是根目录 CLAUDE.md：**

在 `## 📜 变更日志` 板块添加一行：

```
| {日期} | {变更类型} | {变更内容} | {影响范围} |
```

**变更类型：**
- 文件夹-新增/删除/重命名/移动
- 工作流-新增/修改/删除
- Skill-新增/修改/删除
- 规则-新增/修改/删除

### 步骤 7：调用 sync-structure 检查影响

自动调用 `/qy-sync-structure`，检查此次修改是否影响：
- 工作流文档
- Skills
- 其他引用文件

### 步骤 8：生成修改报告

```
## CLAUDE.md 修改报告

### 修改位置
- {根目录 or 项目级 or 领域级}

### 修改内容
- {简述}

### 备份位置
- {备份文件路径}

### 影响评估
- {受影响的文件列表}

### 下一步
- [ ] 检查受影响的文件是否需要更新
- [ ] 测试相关 Skills 是否正常工作

✅ CLAUDE.md 已更新
✅ 旧版本已备份
✅ 变更日志已记录
```

---

## 使用场景

### 根目录 CLAUDE.md 修改场景
1. 系统架构变更（新增/删除核心文件夹）
2. 新增/删除 Skill
3. 反模式清单更新
4. 核心规则调整

### 子文件夹 CLAUDE.md 修改场景
1. 项目特定规则（命名规范、工作流程）
2. 领域特定规则（发布前检查清单）
3. 个人使用习惯

---

## 安全机制

1. **强制备份** - 修改前必须备份
2. **规则验证** - 不符合"何时修改"规则时提示
3. **影响检查** - 自动调用 sync-structure
4. **变更记录** - 所有修改记录到变更日志

---

## 注意事项

1. **不要跳过验证步骤** - 每次修改都要走完整流程
2. **修改后立即测试** - 验证相关 Skills 是否正常
3. **保留备份** - 至少保留最近 3 个版本的备份
